<?xml version="1.0" encoding="utf-8"?>

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/fragmentTimer">

    <fragment
        android:id="@+id/fragmentTimer"
        android:name="com.example.pomodorotechnique.FragmentTimer"
        android:label="FragmentTimer" />
    <fragment
        android:id="@+id/fragmentHistory"
        android:name="com.example.pomodorotechnique.screens.history.FragmentHistory"
        android:label="fragment_history2"
        tools:layout="@layout/fragment_history" >
        <action
            android:id="@+id/action_fragmentHistory_to_fragmentTimer"
            app:destination="@id/fragmentTimer" />
    </fragment>
</navigation>